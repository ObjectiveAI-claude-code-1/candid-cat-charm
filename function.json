{
  "type": "vector.function",
  "input_schema": {
    "type": "array",
    "description": "A collection of cat pictures to rank by unselfconscious charm.",
    "minItems": 2,
    "items": {
      "type": "image",
      "description": "A picture of a cat to evaluate for the naturalness and authenticity of the captured moment."
    }
  },
  "output_length": {
    "$starlark": "len(input)"
  },
  "input_split": {
    "$starlark": "[[item] for item in input]"
  },
  "input_merge": {
    "$starlark": "[item for sublist in input for item in sublist]"
  },
  "tasks": [
    {
      "type": "placeholder.scalar.function",
      "input_schema": {
        "type": "image",
        "description": "A single cat picture to evaluate for the genuineness and authenticity of the cat's observed behavior."
      },
      "skip": null,
      "map": 0,
      "input": {
        "$starlark": "map"
      },
      "output": {
        "$starlark": "[x / sum(output) if sum(output) > 0 else 1.0 / len(output) for x in output]"
      }
    },
    {
      "type": "placeholder.vector.function",
      "input_schema": {
        "type": "array",
        "description": "A collection of cat pictures to rank by emotional resonance.",
        "minItems": 2,
        "items": {
          "type": "image",
          "description": "A cat picture to evaluate for the emotional weight and genuine feeling it evokes in the viewer."
        }
      },
      "output_length": {
        "$starlark": "len(input)"
      },
      "input_split": {
        "$starlark": "[[item] for item in input]"
      },
      "input_merge": {
        "$starlark": "[item for sublist in input for item in sublist]"
      },
      "skip": null,
      "input": {
        "$starlark": "input"
      },
      "output": {
        "$starlark": "output"
      }
    },
    {
      "type": "placeholder.vector.function",
      "input_schema": {
        "type": "array",
        "description": "A collection of cat pictures to rank by spontaneous aliveness.",
        "minItems": 2,
        "items": {
          "type": "image",
          "description": "A cat picture to evaluate for spontaneity and the quality of a seized, unplanned moment."
        }
      },
      "output_length": {
        "$starlark": "len(input)"
      },
      "input_split": {
        "$starlark": "[[item] for item in input]"
      },
      "input_merge": {
        "$starlark": "[item for sublist in input for item in sublist]"
      },
      "skip": null,
      "input": {
        "$starlark": "input"
      },
      "output": {
        "$starlark": "output"
      }
    }
  ],
  "input_maps": [
    {
      "$starlark": "input"
    }
  ],
  "description": "Ranks cat pictures by unselfconscious charm, evaluating behavioral authenticity, emotional resonance, and spontaneous aliveness to surface images that capture genuine, unposed moments of cats being themselves over staged or contrived content."
}